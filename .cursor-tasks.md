Okay, understood. Here is the highly detailed markdown checklist derived from the provided plan, designed for an AI coding agent to execute. Each task corresponds to a small, actionable step.

```markdown
# Detailed Project Checklist: ASCII Art Portfolio Website (AI Agent Task List)

## EPIC 1: Project Setup & Environment

**Feature 1.1: Clean and Configure Next.js Starter**

*   [ ] **(1) Remove Unused Pages & APIs:**
    *   [ ] Identify specific folders/files related to authentication (e.g., `pages/api/auth`, `components/auth`, `lib/auth`) in the `next-ai-starter` template.
    *   [ ] Delete these authentication-related folders and files using `rm -rf`.
    *   [ ] Identify specific folders/files related to Prisma/database (e.g., `prisma/`, relevant API routes).
    *   [ ] Delete these Prisma-related folders and files using `rm -rf`.
    *   [ ] Identify any example pages or components not needed (e.g., `pages/_examples`, example components).
    *   [ ] Delete these example folders and files using `rm -rf`.
    *   [ ] Review `pages/api/` and remove any unused default API routes.
    *   [ ] Review `components/` and remove any unused default components.
*   [ ] **(2) Uninstall Unneeded Packages:**
    *   [ ] Identify unnecessary packages in `package.json` (e.g., `next-auth`, `@prisma/client`, `prisma`, potentially UI libraries if not used).
    *   [ ] Run `npm uninstall next-auth @prisma/client prisma [other-packages...]` (or `yarn remove ...`).
    *   [ ] Verify `package-lock.json` (or `yarn.lock`) is updated.
*   [ ] **(3) Validate Build:**
    *   [ ] Run `npm install` (or `yarn`) to ensure dependencies are clean.
    *   [ ] Run `npm run build` (or `yarn build`).
    *   [ ] Check build output for any errors related to removed code or packages. Resolve if necessary.

**Feature 1.2: Notion SDK Integration & Env Setup**

*   [ ] **(4) Install @notionhq/client:**
    *   [ ] Run `npm install @notionhq/client` (or `yarn add @notionhq/client`).
    *   [ ] Verify the package is added to `package.json`.
*   [ ] **(5) Create .env.local with NOTION_API_KEY and NOTION_BLOG_DATABASE_ID:**
    *   [ ] Create a new file named `.env.local` in the project root.
    *   [ ] Add the line `NOTION_API_KEY=YOUR_SECRET_NOTION_API_KEY`. (Replace with actual key placeholder).
    *   [ ] Add the line `NOTION_BLOG_DATABASE_ID=YOUR_NOTION_DATABASE_ID`. (Replace with actual ID placeholder).
    *   [ ] Ensure `.env.local` is listed in the project's `.gitignore` file.
*   [ ] **(6) Set Vercel Env Vars: NOTION_API_KEY, NOTION_BLOG_DATABASE_ID:**
    *   [ ] Access the Vercel project dashboard.
    *   [ ] Navigate to Settings > Environment Variables.
    *   [ ] Add a variable named `NOTION_API_KEY` with the actual Notion API key value. Ensure it's set for Production, Preview, and Development environments.
    *   [ ] Add a variable named `NOTION_BLOG_DATABASE_ID` with the actual Notion Database ID value. Ensure it's set for Production, Preview, and Development environments.
*   [ ] **(7) Notion DB Access:**
    *   [ ] Go to the Notion Integration settings page (where the API key was generated).
    *   [ ] Find the specific integration created for this project.
    *   [ ] Go to the Notion Database intended for the blog.
    *   [ ] Click the 'Share' button on the database.
    *   [ ] Invite the integration created above, ensuring it has at least 'Read' permissions.

## EPIC 2: ASCII Screensaver & Animations

**Feature 2.1: Basic ASCII Screensaver Loop**

*   [ ] **(8) Create AsciiBackground component with `<pre id="ascii-canvas">`:**
    *   [ ] Create file `components/AsciiBackground.tsx`.
    *   [ ] Implement a basic React functional component structure.
    *   [ ] Include `<pre id="ascii-canvas" aria-hidden="true" style={{ /* basic inline styles for testing: */ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 1, whiteSpace: 'pre', fontFamily: 'monospace', margin: 0, padding: 0, overflow: 'hidden', pointerEvents: 'none', userSelect: 'none' }}></pre>` in the returned JSX.
    *   [ ] Add a `useRef` hook for the `<pre>` element: `const preRef = useRef<HTMLPreElement>(null);`. Assign it: `<pre ref={preRef} ...>`.
*   [ ] **(9) Initialize Loop:**
    *   [ ] Add `useEffect` hook to `AsciiBackground.tsx`.
    *   [ ] Inside `useEffect`, check `if (typeof window === 'undefined') return;`.
    *   [ ] Implement a `requestAnimationFrame` loop structure. Define a function `animate(timestamp)`.
    *   [ ] Store the animation frame ID: `let frameId: number;`. Call `frameId = requestAnimationFrame(animate);`.
    *   [ ] Add a cleanup function in `useEffect` to call `cancelAnimationFrame(frameId);` on unmount.
    *   [ ] *(Alternative: If reusing Gysin's engine)* Import `boot` from `js/engine.js` (copy engine file first). Call `boot({ element: preRef.current, fps: 30, /* other options */ })` inside `useEffect`. Ensure engine handles cleanup.
*   [ ] **(10) Single DOM Update:**
    *   [ ] Inside the `animate` function (or engine's render step):
    *   [ ] Calculate the dimensions (rows/cols) based on `preRef.current.offsetWidth` / `offsetHeight` and character size (estimate for now).
    *   [ ] Generate a placeholder ASCII string representing the full grid (e.g., looping through rows/cols, appending chars).
    *   [ ] Check if `preRef.current` exists.
    *   [ ] Update content: `preRef.current.textContent = fullFrameString;`.
*   [ ] **(11) 30 FPS:**
    *   [ ] Inside the `animate` function:
        *   [ ] Add timestamp tracking: store `lastTimestamp`. Calculate `elapsedTime = timestamp - lastTimestamp;`.
        *   [ ] Only run the grid generation and DOM update if `elapsedTime > 33` (approx. 30 FPS). Update `lastTimestamp = timestamp;`.
        *   [ ] Always call `frameId = requestAnimationFrame(animate);` at the end to keep the loop going.
    *   [ ] *(Alternative: If reusing Gysin's engine)* Ensure `{ fps: 30 }` option is passed during `boot`.

**Feature 2.2: Word-Cycling & Transitions**

*   [ ] **(12) Word List:**
    *   [ ] Define `const defaultWords = ["HELLO", "WORLD", "ASCII"];` within the scope of the animation logic (`AsciiBackground` or the engine).
*   [ ] **(13) userWord:**
    *   [ ] Add a prop `userWord?: string` to the `AsciiBackground` component.
    *   [ ] In the animation logic, check if `userWord` prop is provided and valid. If yes, use it; otherwise, use `defaultWords`.
*   [ ] **(14) Transition:**
    *   [ ] Add state/variables: `currentWordIndex`, `isTransitioning`, `transitionProgress` (0 to 1).
    *   [ ] Modify the render logic: When `isTransitioning` is true, calculate character based on `transitionProgress` (e.g., randomly select char, or blend between old/new char codes). Increment `transitionProgress`. When it reaches 1, set `isTransitioning = false`.
    *   [ ] Trigger transition: When the frame count indicates a word change, set `isTransitioning = true`, `transitionProgress = 0`, update `currentWordIndex`.
*   [ ] **(15) Validate cycle frequency:**
    *   [ ] Add `frameCount` state/variable, incremented each rendered frame.
    *   [ ] Use `if (frameCount % 300 === 0)` (or similar interval) to trigger the word change logic (set `isTransitioning = true`, etc.).

**Feature 2.3: Sand Game With Letters**

*   [ ] **(16) animations/sandGame.ts:**
    *   [ ] Create `lib/animations/sandGame.ts` (or similar path).
    *   [ ] Define data structure for the grid (e.g., `string[][]` or `number[][]` if using char codes).
    *   [ ] Implement `updateSand(grid: string[][]): string[][]` function.
    *   [ ] Inside `updateSand`, iterate grid bottom-up, right-to-left. For each cell containing a letter ('sand'):
        *   [ ] Check cell below (`y+1`). If empty, move letter down.
        *   [ ] If below is occupied, check bottom-left (`x-1, y+1`) and bottom-right (`x+1, y+1`). Move diagonally if empty (randomly pick one if both are empty).
        *   [ ] Update the grid state. Return the new grid.
*   [ ] **(17) Click Handling:**
    *   [ ] Add `onClick` handler to the main container element wrapping `<pre>` in `AsciiBackground`.
    *   [ ] In the handler, get `event.clientX`, `event.clientY`.
    *   [ ] Get bounding rect of `<pre>`: `preRef.current.getBoundingClientRect()`.
    *   [ ] Calculate relative coords: `relativeX = clientX - rect.left`, `relativeY = clientY - rect.top`.
    *   [ ] Calculate grid coords: `gridX = Math.floor(relativeX / charWidth)`, `gridY = Math.floor(relativeY / charHeight)` (Need `charWidth`, `charHeight` calculations).
    *   [ ] Add the next letter (from word cycling) to the internal grid state at `grid[gridY][gridX]`.
*   [ ] **(18) Word Cycling (for Sand):**
    *   [ ] Add state/variable `currentSandLetterIndex`.
    *   [ ] Determine the active word (`userWord` or `defaultWords[currentWordIndex]`).
    *   [ ] Get the letter to spawn: `letter = activeWord[currentSandLetterIndex]`.
    *   [ ] Place `letter` on the grid during click handling.
    *   [ ] Increment `currentSandLetterIndex` modulo `activeWord.length`.
*   [ ] **(19) Collision:**
    *   [ ] Ensure the `updateSand` logic correctly handles occupied cells, preventing letters from moving into already filled spots below or diagonally below.

**Feature 2.4: (Optional) Additional Patterns**

*   [ ] **(20) Implement Background Pattern:**
    *   [ ] In the main animation render logic (where the grid string is built):
    *   [ ] For each cell `(x, y)`:
        *   [ ] First, check if a word character should be drawn there. If yes, use it.
        *   [ ] Else, check if a sand character exists there in the sand grid state. If yes, use it.
        *   [ ] Else, calculate a background pattern character (e.g., using `Math.sin`, Perlin noise `noise(x * scale, y * scale, time * speed)` mapped to characters `.`, `*`, `+`).
        *   [ ] Append the chosen character to the `fullFrameString`.

## EPIC 3: Overlay UI & Navigation

**Feature 3.1: ASCII Overlay Structure**

*   [ ] **(21) Create `<AsciiOverlay>`:**
    *   [ ] Create file `components/AsciiOverlay.tsx`.
    *   [ ] Implement basic React functional component structure.
    *   [ ] Return `<div className="overlay"> ... </div>`.
    *   [ ] Inside the div, add `<h2>` elements for categories (e.g., "Navigation", "External").
    *   [ ] Add `<ul>` and `<li>` for links under each category.
*   [ ] **(22) Nav Links:**
    *   [ ] Use Next.js `<Link>` component for internal navigation: `<Link href="/">[Home]</Link>`, `<Link href="/about">[About]</Link>`, `<Link href="/blog">[Blog]</Link>`, `<Link href="/contact">[Contact]</Link>`.
    *   [ ] Use standard `<a>` tags with `target="_blank" rel="noopener noreferrer"` for external links (GitHub, LinkedIn). Use *your* personal links.
*   [ ] **(23) Position Overlay:**
    *   [ ] Create/Edit `styles/globals.css` (or a dedicated CSS module).
    *   [ ] Add CSS rule `.overlay { position: relative; z-index: 10; padding: 2ch; /* Add more styling as needed */ }`.
    *   [ ] Ensure the `<pre>` tag CSS in `AsciiBackground` (or global CSS) has `z-index: 1;` (or lower than 10).
    *   [ ] Import and use `<AsciiOverlay />` in the main layout (`app/layout.tsx`).

**Feature 3.2: Hover-Interactive Styling**

*   [ ] **(24) Add Hover CSS:**
    *   [ ] In global CSS or overlay's CSS module:
    *   [ ] Add `.overlay a { color: #ccc; /* Or your base text color */ text-decoration: none; display: inline-block; /* Optional: better background */ padding: 0 0.5ch; /* Optional */ }`.
    *   [ ] Add `.overlay a:hover { color: #000; background-color: #0f0; /* Or desired highlight */ transition: background-color 0.1s, color 0.1s; }`.
*   [ ] **(25) Test Hover Effect:**
    *   [ ] Run the dev server (`npm run dev`).
    *   [ ] Open the site in the browser.
    *   [ ] Hover over each link in the overlay and verify the style changes and reverts correctly.

**Feature 3.3: Intro Text & Email**

*   [ ] **(26) Add Intro Paragraph:**
    *   [ ] In `components/AsciiOverlay.tsx`, add a `<p>` tag with your introductory text, styled appropriately (monospace font will be inherited).
*   [ ] **(27) Implement Email Tiroir Effect:**
    *   [ ] Make `<AsciiOverlay>` a client component (`'use client';`).
    *   [ ] Add `useState` for the current subject: `const [emailSubject, setEmailSubject] = useState('Hello');`.
    *   [ ] Define a list of subjects: `const subjects = ['Hi', 'Yo', 'Inquiry', 'Just saying hello', ...];`.
    *   [ ] Define or import a `shuffle` function.
    *   [ ] Add `useEffect` hook:
        *   [ ] Define `swapSubject()` function: selects next subject from shuffled list, updates state via `setEmailSubject`.
        *   [ ] Call `swapSubject()` once initially.
        *   [ ] Set interval: `const intervalId = setInterval(swapSubject, 9000);`.
        *   [ ] Return cleanup function: `return () => clearInterval(intervalId);`.
    *   [ ] Render the email link: `<a href={\`mailto:your.email@example.com?subject=\${encodeURIComponent(emailSubject)}\`}>ciao</a>` (or your preferred link text).
*   [ ] **(28) Confirm mailto Link:**
    *   [ ] Click the rendered email link in the browser.
    *   [ ] Verify that the default email client opens with the correct recipient and the currently displayed subject line.

## EPIC 4: Responsive Design & Layout

**Feature 4.1: Auto-Resizing the ASCII Canvas**

*   [ ] **(29) Implement Auto-Resize Logic:**
    *   [ ] In `AsciiBackground.tsx`'s `useEffect`:
        *   [ ] Define `handleResize()` function. Inside, recalculate character dimensions (`charWidth`, `charHeight`) if needed, recalculate `cols` and `rows` based on `preRef.current` dimensions, potentially re-initialize or clear animation state if dimensions change significantly.
        *   [ ] Call `handleResize()` once initially.
        *   [ ] Add event listener: `window.addEventListener('resize', handleResize);`.
        *   [ ] Return cleanup function to remove listener: `return () => window.removeEventListener('resize', handleResize);`.
    *   [ ] *(Alternative: If using Gysin's engine)* Verify if the engine has built-in resize handling when `{ cols: 0, rows: 0 }` is used. If not, manually trigger re-initialization on resize.
*   [ ] **(30) Test Resizing:**
    *   [ ] Open the site in the browser.
    *   [ ] Open developer tools.
    *   [ ] Resize the browser window width and height significantly.
    *   [ ] Verify the ASCII animation continues to fill the viewport without creating scrollbars on the `<pre>` element.
    *   [ ] Check the mobile view in dev tools.

**Feature 4.2: Mobile-Friendly Overlay**

*   [ ] **(31) Ensure Overlay Scrolling:**
    *   [ ] Check the CSS for the main page layout (`app/layout.tsx` or global styles). Ensure the `<body>` or main content container allows normal document scrolling (`overflow: auto;` or default).
    *   [ ] In `styles/globals.css`, potentially reset `body { overflow: hidden; }` added earlier, or manage overflow more carefully per page. Let `post.html` scroll, and let main pages scroll if overlay content is too long for viewport.
    *   [ ] Add sufficient `padding` to the `.overlay` container (already done in task 23, verify it's adequate).
*   [ ] **(32) Confirm Mobile Layout:**
    *   [ ] Use browser dev tools mobile simulator (e.g., iPhone SE, Pixel 5).
    *   [ ] Verify overlay text doesn't touch screen edges.
    *   [ ] Verify content can be scrolled vertically if it exceeds the screen height.
    *   [ ] Verify columns (if using grid/flex) stack or rearrange appropriately on narrow screens (may need media queries from Feature 4.3).

**Feature 4.3: Responsive Font & Media Queries**

*   [ ] **(33) Add Font Size Media Query:**
    *   [ ] In `styles/globals.css`, add:
        ```css
        @media (max-width: 480px) { /* Adjust breakpoint as needed */
          html {
            font-size: 0.9rem; /* Or adjust base font size */
          }
          /* Potentially add specific adjustments for .overlay padding/margins */
        }
        ```
*   [ ] **(34) Check Line Height:**
    *   [ ] Test on mobile view. If reducing `font-size` causes characters to misalign vertically, adjust `line-height` within the media query (e.g., `line-height: 1;` might still be correct).

## EPIC 5: Notion-Integrated Blog

**Feature 5.1: Blog Listing from Notion**

*   [ ] **(35) Create `lib/notion.ts` and `getPublishedPostsList`:**
    *   [ ] Create file `lib/notion.ts`.
    *   [ ] Add imports: `import { Client } from "@notionhq/client";`.
    *   [ ] Initialize client: `const notion = new Client({ auth: process.env.NOTION_API_KEY });`.
    *   [ ] Define `async function getPublishedPostsList()`.
    *   [ ] Inside, call `notion.databases.query({ database_id: process.env.NOTION_BLOG_DATABASE_ID!, ... })`.
*   [ ] **(36) Add Filter and Map Results:**
    *   [ ] Add `filter: { property: 'Status', status: { equals: 'Published' } }` to the query (adjust property name 'Status' based on your Notion DB).
    *   [ ] Add `sorts: [{ property: 'Date', direction: 'descending' }]` (adjust property name 'Date').
    *   [ ] Map `results` to return an array of objects like `{ id: page.id, title: page.properties.Name.title[0]?.plain_text || '', slug: page.properties.Slug.rich_text[0]?.plain_text || '', date: page.properties.Date.date?.start || '', excerpt: page.properties.Excerpt.rich_text[0]?.plain_text || '' }` (adjust property names).
*   [ ] **(37) Create `app/blog/page.tsx`:**
    *   [ ] Create the file `app/blog/page.tsx`.
    *   [ ] Make it an async server component.
    *   [ ] Call `const posts = await getPublishedPostsList();` inside the component.
    *   [ ] Render a list (`<ul>` or divs).
    *   [ ] Map over `posts`, rendering `<Link href={\`/blog/\${post.slug}\`}><h2>{post.title}</h2></Link>`, `<p>{post.date}</p>`, `<p>{post.excerpt}</p>` for each post. Style minimally in ASCII theme.

**Feature 5.2: Individual Post Pages**

*   [ ] **(38) Add `getPostContent` to `lib/notion.ts`:**
    *   [ ] Define `async function getPostContent(pageId: string)`.
    *   [ ] Inside, call `notion.blocks.children.list({ block_id: pageId });`.
    *   [ ] Return `results` (the list of blocks).
    *   [ ] *(Optional but recommended):* Also fetch page properties to get Title: `notion.pages.retrieve({ page_id: pageId })`. Return `{ blocks: results, title: pageProperties.properties.Name.title[0]?.plain_text }`.
*   [ ] **(39) Create `app/blog/[slug]/page.tsx`:**
    *   [ ] Create file `app/blog/[slug]/page.tsx`.
    *   [ ] Implement `generateStaticParams` function: fetch all slugs using `getPublishedPostsList`, return array like `[{ slug: 'post-one' }, ...]`.
    *   [ ] Implement the async page component `BlogPostPage({ params }: { params: { slug: string } })`.
    *   [ ] Inside, fetch the specific page ID based on `params.slug` (requires another query or finding from the list fetched in `generateStaticParams`). Handle case where slug doesn't match (show 404 using `notFound()` from `next/navigation`).
    *   [ ] Fetch content: `const { blocks, title } = await getPostContent(pageId);`.
*   [ ] **(40) Create and Use `NotionBlockRenderer`:**
    *   [ ] Create `components/NotionBlockRenderer.tsx`.
    *   [ ] Define component accepting `blocks: any[]` prop.
    *   [ ] Implement mapping logic: iterate `blocks`, use `switch(block.type)` to render corresponding HTML elements (`p`, `h1`/`h2`/`h3`, `ul`/`li`, `pre`/`code`). Handle basic `rich_text` formatting (bold, italic, links). Use basic inline styles or CSS classes for ASCII theme.
    *   [ ] In `app/blog/[slug]/page.tsx`, render `<NotionBlockRenderer blocks={blocks} />`. Display the `title` in an `<h1>`.

**Feature 5.3: Incremental Static Regeneration**

*   [ ] **(41) Add `revalidate` Constant:**
    *   [ ] In `app/blog/page.tsx`, add `export const revalidate = 3600;` (or desired interval in seconds).
    *   [ ] In `app/blog/[slug]/page.tsx`, add `export const revalidate = 3600;`.
*   [ ] **(42) Test Revalidation (Post-Deployment):**
    *   [ ] Deploy the site to Vercel.
    *   [ ] Access the blog index and a specific post page.
    *   [ ] Edit the content or publish status of that post in Notion.
    *   [ ] Wait for the `revalidate` interval (e.g., 1 hour) *plus* a few minutes.
    *   [ ] Refresh the blog index and the post page on the live site.
    *   [ ] Verify the changes are reflected.

## EPIC 6: Search Functionality

**Feature 6.1: Client-Side Filter**

*   [ ] **(43) Add Input and State:**
    *   [ ] Add `'use client';` at the top of `app/blog/page.tsx`.
    *   [ ] Import `useState` from `react`.
    *   [ ] Add state: `const [searchQuery, setSearchQuery] = useState('');`.
    *   [ ] Add input element: `<input type="text" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder="Search posts..." />`.
*   [ ] **(44) Implement Filter Logic:**
    *   [ ] Before mapping over `posts` to render the list:
    *   [ ] Add `const filteredPosts = posts.filter(post => post.title.toLowerCase().includes(searchQuery.toLowerCase()) || post.excerpt.toLowerCase().includes(searchQuery.toLowerCase()));`.
*   [ ] **(45) Render Filtered List:**
    *   [ ] Map over `filteredPosts` instead of `posts` when rendering the list items.

**Feature 6.2: ASCII-Style Search Box**

*   [ ] **(46) Add Search Input CSS:**
    *   [ ] Add a specific class to the search input, e.g., `className="ascii-search"`.
    *   [ ] In CSS: `.ascii-search { font-family: monospace; background-color: #111; color: #0f0; border: 1px solid #333; padding: 0.5ch 1ch; /* Adjust appearance */ }`.
    *   [ ] Style `::placeholder` for the input: `.ascii-search::placeholder { color: #555; }`.
*   [ ] **(47) Add Optional Label:**
    *   [ ] Place a `<span>` before the input if desired: `<label><span>[Search: ]</span> <input ... /></label>`. Style the label/span as needed.

## EPIC 7: Light & Dark Mode

**Feature 7.1: Theme Toggle**

*   [ ] **(48) Install `next-themes`:**
    *   [ ] Run `npm install next-themes` (or `yarn add next-themes`).
*   [ ] **(49) Add ThemeProvider:**
    *   [ ] Create `components/ThemeProvider.tsx`. Import `ThemeProvider` from `next-themes`. Re-export it as a client component.
    *   [ ] In `app/layout.tsx`, import your `ThemeProvider` and wrap the `{children}` with `<ThemeProvider attribute="class" defaultTheme="dark">{children}</ThemeProvider>`.
*   [ ] **(50) Create and Add Toggle Component:**
    *   [ ] Create `components/ThemeToggle.tsx`.
    *   [ ] Add `'use client';`. Import `useTheme` from `next-themes`.
    *   [ ] Inside component, get `{ theme, setTheme, resolvedTheme } = useTheme();`.
    *   [ ] Render a button or link: `<button onClick={() => setTheme(resolvedTheme === 'dark' ? 'light' : 'dark')}>[Toggle Theme]</button>`.
    *   [ ] Add `<ThemeToggle />` inside `components/AsciiOverlay.tsx`.
*   [ ] **(51) Verify Persistence:**
    *   [ ] Run dev server. Toggle theme. Refresh page. Verify the chosen theme remains active.

**Feature 7.2: Animations in Both Themes**

*   [ ] **(52) Pass Theme to Animation:**
    *   [ ] In `app/layout.tsx` or where `<AsciiBackground>` is rendered, if possible make it a client component or pass theme down. Get theme using `useTheme` hook.
    *   [ ] Pass the `resolvedTheme` as a prop to `<AsciiBackground theme={resolvedTheme}>`.
*   [ ] **(53) Adjust Animation Colors:**
    *   [ ] Modify the animation logic (in `AsciiBackground` or engine) to accept the `theme` prop.
    *   [ ] Define color palettes: `const darkColors = { bg: '#000', char: '#0f0' }; const lightColors = { bg: '#fff', char: '#333' };`.
    *   [ ] Select colors based on theme: `const colors = theme === 'dark' ? darkColors : lightColors;`.
    *   [ ] Use `colors.char` for rendering ASCII characters. Use `colors.bg` for the `<pre>` background style (might need dynamic style update).

## EPIC 8: Contact & About Pages

**Feature 8.1: About Page with Sand Game**

*   [ ] **(54) Create `app/about/page.tsx`:**
    *   [ ] Create the file. Add basic page structure.
    *   [ ] Include `<AsciiOverlay />` (or relevant parts like nav).
    *   [ ] Add `<p>` tags with your bio/about text inside the overlay structure.
    *   [ ] Render `<AsciiBackground mode="sand" />` (or similar prop) to activate sand game logic.
*   [ ] **(55) Enable Conditional Sand Logic:**
    *   [ ] Modify `AsciiBackground` component to accept `mode` prop.
    *   [ ] Inside the animation loop/setup, check `if (props.mode === 'sand')`. If true, initialize and run the sand game logic (including click handler). Otherwise, run default word cycling/patterns.

**Feature 8.2: Contact Page with Game of Life**

*   [ ] **(56) Create `app/contact/page.tsx` and GOL Logic:**
    *   [ ] Create `app/contact/page.tsx`. Include `<AsciiOverlay />`.
    *   [ ] Implement Conway's Game of Life logic (e.g., in `lib/animations/gameOfLife.ts`). Function `updateGOL(grid)` takes current grid, applies rules, returns next grid state.
    *   [ ] Modify `AsciiBackground` to handle `mode="gol"`. Initialize a GOL grid state, call `updateGOL` in the animation loop, render the GOL state to the `<pre>`.
    *   [ ] Render `<AsciiBackground mode="gol" />` in the contact page component.
*   [ ] **(57) Add Contact Info:**
    *   [ ] Inside the `<AsciiOverlay>` rendering specific to the contact route, add your contact email link (potentially the 'tiroir' one) and/or other contact details.

**Feature 8.3: Email Address Obfuscation (Optional)**

*   [ ] **(58) Implement Obfuscation:**
    *   [ ] If desired, replace the direct `mailto:` link on the contact page.
    *   [ ] Render email parts separately: `<span>your.email [at] example [dot] com</span>`.
    *   [ ] Or, add an `onClick` handler to a button/link that dynamically constructs and sets `window.location.href = 'mailto:...'`.

## EPIC 9: Performance, QA & Deployment

**Feature 9.1: Single DOM Update & ~30 FPS Check**

*   [ ] **(59) Code Review Animation Loop:**
    *   [ ] Manually inspect the `animate` function or engine's render step in `AsciiBackground`.
    *   [ ] Confirm there is only one `preRef.current.textContent = ...` call per visual frame update.
*   [ ] **(60) Verify FPS Throttling:**
    *   [ ] Check the `requestAnimationFrame` time logic or the `{ fps: 30 }` engine option. Ensure it's correctly implemented.
*   [ ] **(61) Profile Performance:**
    *   [ ] Open Chrome DevTools > Performance tab. Record ~30 seconds of the site running. Analyze the timeline for CPU usage spikes and average FPS. Aim for stable ~30 FPS and reasonable CPU load.

**Feature 9.2: Cross-Browser QA**

*   [ ] **(62) Manual Cross-Browser Testing:**
    *   [ ] Open the latest version of Google Chrome. Test all pages and features.
    *   [ ] Open the latest version of Mozilla Firefox. Test all pages and features.
    *   [ ] Open the latest version of Apple Safari. Test all pages and features.
    *   [ ] Use browser dev tools to simulate mobile viewports for each browser. Test core features.
*   [ ] **(63) Document Issues:**
    *   [ ] Create a simple list or use an issue tracker. Note any rendering bugs (misalignment, broken layout), console errors, or functional differences observed between browsers.

**Feature 9.3: Final Deploy on Vercel**

*   [ ] **(64) Link Repo to Vercel:**
    *   [ ] Log in to Vercel. Create a 'New Project'.
    *   [ ] Import the Git repository containing the project.
    *   [ ] Configure framework preset (should detect Next.js). Set Root Directory if needed.
*   [ ] **(65) Double-Check Vercel Env Vars:**
    *   [ ] Navigate to the newly created Vercel project settings > Environment Variables.
    *   [ ] Confirm `NOTION_API_KEY` and `NOTION_BLOG_DATABASE_ID` exist and have the correct values for the Production environment.
*   [ ] **(66) Monitor First Build:**
    *   [ ] Trigger the first deployment (usually automatic after linking repo).
    *   [ ] Go to the 'Deployments' tab in Vercel. Monitor the build logs in real-time.
    *   [ ] Ensure the build completes without errors (pay attention to Notion fetching during build if using SSG/ISR).
*   [ ] **(67) Production Smoke Test:**
    *   [ ] Access the main production URL provided by Vercel (e.g., `your-project.vercel.app`).
    *   [ ] Click through all navigation links (Home, About, Blog, Contact).
    *   [ ] Verify ASCII background animation runs on main pages.
    *   [ ] Verify overlay content is correct.
    *   [ ] Navigate to the blog index, verify posts load.
    *   [ ] Click a blog post link, verify content loads from Notion.
    *   [ ] Test search filter on blog index.
    *   [ ] Test theme toggle.
    *   [ ] Test sand game interaction on About page (if applicable).
    *   [ ] Check browser console for any errors on the live site.

```